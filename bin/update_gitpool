#!/usr/bin/env bash

code="$HOME/codespace/opensource"

for p in $(ls $code)
do
	if ! cd "$code/$p" &>/dev/null; then
		continue
	fi
	if [ ! -d .git ]; then
		continue
	fi
	git remote update 2>/dev/null | sed '1d'
done
