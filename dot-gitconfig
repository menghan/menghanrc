[alias]
	co = checkout
	ci = commit -a -v
	cv = commit -v
	st = status
	br = branch
	cl = clean -f -d
	ru = remote update
	rs = remote show origin
	recent = log --abbrev-commit --pretty=oneline -10
	last = log -p -1
	tco = diff --staged	# to commit
	to = diff --staged	# to commit
	prd = !git remote update upstream > /dev/null && git diff -w `git merge-base HEAD upstream/master`
	pa = push --all
# borrow from liuyie begins
	sb = show-branch
	gf = log --graph --oneline --decorate
# borrow from liuyie ends

[color]
	status = auto
	branch = auto
	ui = auto
[core]
	pager = less -FRSX
[user]
	name = Menghan
	email = menghan412@gmail.com
[log]
	date = local
[blame]
	date = local

[credential]
	helper = osxkeychain
[svn]
	pushmergeinfo = true
